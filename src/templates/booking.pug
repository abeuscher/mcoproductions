extends inc/page.pug

block page_id
  each page in locals.pages
    if page.title=="Booking"
      - this_page = page

block content
  .content.row.booking
    .col-12-s
      h1=this_page.title
      include ./inc/booking-form.pug
